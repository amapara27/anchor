[package]
name = "anchor-hub"
description = "Unified local-model registry for Anchor (Ollama REST + SQLite cache)."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
anchor-core = { workspace = true }
anchor-system = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
reqwest = { workspace = true }
rusqlite = { workspace = true }
thiserror = { workspace = true }
futures-util = { workspace = true }
tokio = { version = "1", features = ["time", "rt"] }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
# Test-only: pins the bundled catalog's architecture metadata against what a
# live `/api/show` reports. Not a runtime dependency of the registry.
anchor-search = { workspace = true }
